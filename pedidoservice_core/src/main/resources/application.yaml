spring:
  application:
    name: pedidoservice_core
  datasource:
    url: jdbc:postgresql://localhost:5432/logiflow_pedidos
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8080

  logging:
    level:
      com.parkin.users: DEBUG